---
# Claude 起草，事实核对过，口吻待沉麟重写。改完把 draft 去掉。
question: 这个站的代码开源吗？
aliases:
  - 能看到你网站的源码吗
  - 仓库地址是什么
  - 这个站的代码在哪
topic: 这个站
draft: true
---

开源，仓库在 `github.com/DUEDCL/duchenlin-blog`。

里面除了站点代码，还有一个 `docs/` 目录，记着需求原文、技术选型的理由和踩过的坑 ——
比如毛玻璃为什么必须有可折射的底层、两个域名里为什么有一个国内打不开、缓存头为什么不能
用 `/*` 兜底。**那些踩坑记录比代码本身有用**，代码换个框架就重写了，坑是同一个坑。

要拿去改随意。有一处提醒：`wrangler.toml` 里的域名和 Worker 名是我这边的，照抄部署不到
你自己的账号上。
