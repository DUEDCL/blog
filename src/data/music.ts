/**
 * 唱片机的曲目表。
 *
 * 现在这里只有一首：方大同《特别的人》（专辑《危险世界》）。
 * **这是别人的作品** —— R22 站主的原话是「删掉预设的那两首歌，改成方大同的特别的人」，
 * 并自己把 mp3 与 .lrc 下载到桌面交给我当本地素材。原先那两首是我用 ffmpeg 合成的
 * 环境音垫，所以「不是任何人的作品」那句口径与两条合成命令由本轮一并作废。
 *
 * 素材与实测值：
 * - `public/audio/te-bie-de-ren.mp3` 10,365,431 字节，320 kbps / 44.1 kHz / 立体声。
 *   字节数与上游完整版直链的 `content-range: bytes 0-9/10365431` 逐位一致。
 *   `duration` 取 `ffprobe -show_entries format=duration` 的 259.064263 → 列表显示 4:19。
 * - `public/audio/te-bie-de-ren.jpg` 87,364 字节、300×300。从上游 `types=pic` 给出的
 *   网易云直链取一次存成本地文件（`?param=300y300`，URL 里没有 token 也没有时间戳）。
 *   不走 `astro:assets`：同一个 `<img>` 还要在运行期接点歌曲目的远程 URL，放 `public/` 零构建风险。
 * - `lrc` 是桌面那份 .lrc，**剔掉了开头 15 行制作人名单** —— 那 15 行的时间戳是假的
 *   （`[00:00.000]`~`[00:14.000]` 每秒一行：作词/作曲/编曲/制作人/录制/工程师/混音师…），
 *   在逐字淡化的歌词舞台上就是 15 秒噪音。真歌词从 `[00:15.360]` 起原样内联，
 *   前 15 秒于是落在既有的「还在前奏」分支（`k = -1`）。
 *
 * 再加曲目时：mp3 放进 `public/audio/`，改这里的 title/subtitle/src/cover/duration/lrc
 * 就够了，页面与播放器不用动。duration 要填准 —— 播放列表在音频一个字节都没
 * 下载时就要显示时长，靠的是这个值，不是 loadedmetadata。
 */

export interface Track {
  /** 用作 DOM id 与列表 key，也是文件名 */
  id: string;
  title: string;
  /** 标题下的一行小字：出处、写它时在干什么，随意 */
  subtitle: string;
  src: string;
  /**
   * 专辑封面的路径，贴在唱片中心那个圆标签上（R22①）。
   * 留空就退回原来的烫金文字标签 —— 点歌进来的曲目走的正是这条退路：
   * 它们的封面要等 `/api/music/pic` 回来才有，而且取不到也照样能播。
   */
  cover: string;
  /** 秒，填实测值（ffprobe -show_entries format=duration） */
  duration: number;
  /**
   * 歌词，标准 LRC 格式。空字符串表示这首没有词。
   * 点歌进来的曲目不填这里，由 `/api/music/lyric` 在运行期灌进状态。
   */
  lrc: string;
}

export const TRACKS: Track[] = [
  {
    id: 'te-bie-de-ren',
    title: '特别的人',
    subtitle: '方大同 · 危险世界',
    src: '/audio/te-bie-de-ren.mp3',
    cover: '/audio/te-bie-de-ren.jpg',
    duration: 259.06,
    lrc: `[00:15.360]爱一个人或许要慷慨
[00:20.280]若只想要被爱
[00:22.560]最后没有了对白
[00:26.910]必须有你我的情真
[00:31.680]不求计分的平等
[00:34.560]总有幸福有心疼
[00:37.950]生命的起伏要认可
[00:42.780]懂一个人也许要忍耐
[00:47.700]要经过了意外
[00:49.950]才了解所谓的爱
[00:54.300]今后的岁月
[00:56.910]让我们一起了解
[01:00.359]多少天长地久
[01:03.299]有几回细水长流
[01:08.039]我们是对方特别的人
[01:15.299]奋不顾身难舍难分
[01:18.750]不是一般人的认真
[01:22.230]若只有一天爱一个人
[01:28.920]让那时间每一刻在倒退
[01:33.149]生命中有万事的可能
[01:38.190]你就是我要遇见的特别的人
[01:47.910]懂一个人也许要忍耐
[01:52.800]要经过了意外
[01:55.050]才了解所谓的爱
[01:59.429]今后的岁月
[02:02.009]让我们一起了解
[02:05.459]多少天长地久
[02:08.429]有几回细水长流
[02:13.169]我们是对方特别的人
[02:20.429]奋不顾身难舍难分
[02:23.879]不是一般人的认真
[02:27.330]若只有一天爱一个人
[02:34.050]让那时间每一刻在倒退
[02:38.248]生命中有万事的可能
[02:43.379]你就是我要遇见的特别的人
[02:50.429]有时候我们都会寂寞
[02:54.718]有时也会失败怕被淘汰
[02:58.860]想去找一个明白
[03:02.429]而我曾经多次的等待未来
[03:08.459]你何时会来
[03:10.860]人山人海总有你的存在
[03:16.080]有你我的爱
[03:18.360]我们是对方特别的人
[03:25.649]奋不顾身难舍难分
[03:29.039]不是一般人的认真
[03:32.490]若只有一天爱一个人
[03:39.149]让那时间每一刻在倒退
[03:43.438]生命中有万事的可能
[03:48.479]你就是我要遇见的特别的人`,
  },
];

export interface LrcLine {
  /** 秒 */
  time: number;
  text: string;
}

/**
 * 解析标准 LRC。支持一行挂多个时间戳（`[00:01.00][00:30.00]同一句`），
 * 也支持 `.` 与 `:` 两种小数分隔符、两位厘秒与三位毫秒。
 * 无文字的纯时间戳行会被丢掉 —— 渲染出来是个空的 <li>，只会撑乱行距。
 */
export function parseLrc(lrc: string): LrcLine[] {
  const out: LrcLine[] = [];

  for (const raw of lrc.split('\n')) {
    const row = raw.match(/^\s*((?:\[\d+:\d+(?:[.:]\d+)?\]\s*)+)(.*)$/);
    if (!row) continue;

    const text = row[2].trim();
    if (!text) continue;

    for (const stamp of row[1].matchAll(/\[(\d+):(\d+)(?:[.:](\d+))?\]/g)) {
      // '0.' + '50' → 0.5；'0.' + '500' → 0.5。厘秒毫秒都对
      const frac = stamp[3] ? Number('0.' + stamp[3]) : 0;
      out.push({ time: Number(stamp[1]) * 60 + Number(stamp[2]) + frac, text });
    }
  }

  return out.sort((a, b) => a.time - b.time);
}

/**
 * 把一行随片文字切成「能各自点亮的最小单位」：CJK 一字一段，拉丁字母与数字连成的词
 * 整段不切，尾随空白并进前一段。逐字淡化按这些段推进。
 *
 * 为什么需要它：逐字淡化原先在整行上铺一道 `linear-gradient(90deg)`，而 background 的
 * 渐变按元素**盒子**的宽度铺 —— 一行歌词在窄屏折成两个行盒时，两个行盒共用同一道渐变，
 * 于是第二行跟着第一行同步被点亮。切成一个个独立的 inline 盒子之后，每段自己一道渐变，
 * 与折不折行无关。
 *
 * 拉丁词不能拆到字母：每个字母一个 <span> 会在字母之间多出断行机会，
 * 「gold digger」在窄屏会被折成「g / old digger」。CJK 每字本来就可断行，拆开不影响排版。
 * 空白并进前一段是同一个理由 —— 断行机会仍然只落在空格上，不会多出来。
 *
 * `u` 标志是必需的：没有它 `\S` 按 UTF-16 码元匹配，会把 emoji 的代理对切成两半。
 */
export function segment(text: string): string[] {
  const out: string[] = [];
  // 顺序有意义：先试拉丁词（含词内的撇号与连字符），不成再退到单个字符。两者都吃掉尾随空白
  const re = /[A-Za-z0-9]+(?:['’-][A-Za-z0-9]+)*\s*|\S\s*|\s+/gu;
  for (const m of text.matchAll(re)) out.push(m[0]);
  return out;
}

/** 秒 → m:ss。NaN 与负数都退成 0:00，进度条拖到边界时不会显示 NaN:NaN */
export function formatTime(sec: number): string {
  if (!Number.isFinite(sec) || sec < 0) sec = 0;
  const m = Math.floor(sec / 60);
  const s = Math.floor(sec % 60);
  return `${m}:${String(s).padStart(2, '0')}`;
}
