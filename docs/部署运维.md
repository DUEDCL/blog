# 部署运维

## 现状

托管在 **Cloudflare Workers 静态资源模式**（不是 Pages）。**2026-08-22 起不再是纯静态** ——
`wrangler.toml` 有了 `main = "src/worker.ts"`，`/api/music/*` 四条只读 GET 在线上跑
（[R11 点歌台](需求台账.md#r11-点歌台-)），其余路径仍原样交回静态资源。

| 项目 | 值 |
| :--- | :--- |
| Worker 名 | `duchenlin-blog` |
| 绑定域名 | `duchenlin.top`（主域）、`www.duchenlin.top`、`duchenlin.eu.cc`（备用） |
| 服务端代码 | `src/worker.ts`，只有 `/api/music/*`；`run_worker_first = [ "/api/music/*" ]`，其余路径连 Worker 都不唤起 |
| 证书 | Cloudflare 自动签发，两个 zone 均 Active |
| Always Use HTTPS | 两个 zone 均已开启 |
| workers.dev 子域 | **已关闭（2026-08-23）**，连版本预览 URL 一起关。见「已关闭的 workers.dev 入口」 |

DNS 记录和证书都由 `wrangler.toml` 里的 `[[routes]] custom_domain = true` 自动生成，
**不需要手工加任何解析记录**。裸域按 DNS 规范不能用 CNAME，Cloudflare 用 CNAME 展平
（CNAME flattening）在内部解决了这个限制，前提是 DNS 必须托管在 Cloudflare。

## 发布流程

```bash
npm run build && npx wrangler deploy
```

`wrangler deploy` 读的是 `dist/`，所以必须先构建。

**R41 起 `npm run build` 还有第二个产物必须先跑出来**：`scripts/gen-content.mjs` 生成的
`src/data/content.generated.ts`（仓库内容的原样快照，后台的「文章编辑」「图库管理」
就是读它）。它在 `.gitignore` 里，而 `src/worker.ts` **静态引入**它 ——
所以单独跑 `npx wrangler deploy` 而不先 build，会在打包这一步失败（明确报找不到这个 import，
不会静默上错代码）。`npm run dev` 同样会先跑它。

部署需要 `CLOUDFLARE_API_TOKEN` 环境变量，或 `npx wrangler login` 交互授权。

> **凭据实际来自环境变量 `CLOUDFLARE_API_TOKEN`，不是 OAuth 授权。** 这一条 2026-08-23
> 实测纠正了原来的记载（原文写的是「已完成 OAuth 授权，凭据落在 config 文件里」）：
>
> - `npx wrangler whoami` 报的是 *"You are logged in with an User API Token"*；拿
>   `CLOUDFLARE_API_TOKEN` 调 `user/tokens/verify` 返回 `active`，**它才是在起作用的那个**。
> - `%APPDATA%\xdg.config\.wrangler\config\default.toml` 里确实有 `oauth_token`，但它**已经失效** ——
>   拿它调 API 返回 `[{"code":9109,"message":"Invalid access token"}]`。`~/.wrangler` 这个目录不存在。
>
> 所以真正的依赖是那个环境变量：**它一旦没了（换机器、清掉环境）就无法部署**，
> 得去 dashboard 重新签一个 token，光看 config 文件里还躺着 `oauth_token` 会误判成「已授权」。
> Token 权限含 `workers:write`、`ai:write`、`ai-search:write`、`d1:write`、`zone:read`，
> 够 R6 全部阶段用；三个 `custom_domain = true` 要求 `zone:read`，缺了会在 deploy 步失败。
>
> 账号是哪一个**不写在这里** —— 本仓库是公开的，账号邮箱等于把 QQ 号挂到公开索引上。
> 同理，token 只提变量名，不写值。


> **wrangler 走本机代理会失败。** 本机 `HTTP_PROXY` 指向 `127.0.0.1:12334`，该代理本身是通的，
> 但 wrangler 走它报 `fetch failed`，直连 `api.cloudflare.com` 正常。部署前需要清掉
> `HTTP_PROXY HTTPS_PROXY http_proxy https_proxy ALL_PROXY all_proxy`。
>
> PowerShell 里清代理（**`unset` 和 `&&` 都不能用**，那是 POSIX shell 语法）：
>
> ```powershell
> Remove-Item Env:HTTP_PROXY,Env:HTTPS_PROXY,Env:ALL_PROXY -ErrorAction SilentlyContinue
> ```

GitHub 自动构建（Workers Builds）**没接 —— 2026-08-23 查完之后明确决定不接**。所以
「推送 ≠ 上线」这件事继续成立：**GitHub 只是代码存放处，线上跑的是最后一次
`wrangler deploy` 推上去的产物。**

不接的理由都是实测的，不是嫌麻烦：

| 本来指望的收益 | 实测结论 |
| :--- | :--- |
| 非生产分支给 preview 地址，上线前先看一眼 | **打不开。** preview 地址在 `*.workers.dev` 上，本机直连时 DNS 解析到 `162.125.32.13` —— 不属于 Cloudflare 的段（主域拿到的是 `172.67.x` / `104.21.x`），TCP 连不上，两次各 15 秒超时；走本机代理报 `schannel: failed to receive handshake`。同一时刻 `duchenlin.top` 直连 200 / 2.17s |
| 出问题回滚更容易 | **本来就能回滚。** Cloudflare 侧存着 10 个 version（`source` 全是 `wrangler`），指定 version 即可回退，跟 Builds 无关 |
| 省掉手工 `npm run build && npx wrangler deploy` 和清七个代理变量 | 这些命令沉麟一次都没手敲过，一直是 Claude 在跑；而代价（`main` 从此是发布分支、推 commit 等于上线、没法推草稿）由他承担 |

**什么情况下回来接**：沉麟打算自己在 GitHub 网页上改 markdown、提交完就自动上线、
不用叫 Claude —— 那时 Builds 是刚需，preview 打不开也无妨。

真要接的时候按这个填，外加一条**顺序上的死规矩**：

1. **先 `git push` 把本地已部署的内容推上去，再去 dashboard 连接。** 反过来会出事 ——
   2026-08-23 当时 `origin/main` 落后本地 6 个 commit（不含点歌台、不含 `main = "src/worker.ts"`、
   不含 `/` 与 `/home` 的新路由）。先连上、再由任何一次推送触发构建，Cloudflare 会拉那份旧代码，
   **线上直接回退成纯静态站**，起始页与点歌台一起消失。
2. Cloudflare → **Workers & Pages** → `duchenlin-blog` → **Settings** → **Builds** → 连接 `DUEDCL/blog`。
3. 构建命令 `npm run build`，部署命令 `npx wrangler deploy`（后者本来就是默认值），
   分支 `main`，Root directory 留空，Build variables 不填，API token 留默认自动生成。
   构建镜像默认 Node 24，满足 `package.json` 的 `engines: >=22.12.0`，**不用加 `.nvmrc`**。
4. 这一步只能在 dashboard 里点，Claude 做不了 —— GitHub App 的安装授权只能走浏览器。
   Builds 的 REST API 确实存在（`/accounts/{id}/builds/repos/connections` 一类端点返 **403 +
   `documentation_url`** 而不是 404），但当前 token 没有对应权限，而加权限本身也要进 dashboard，
   所以走 API 并不更短。

**由此定下一条约定：每次 `wrangler deploy` 之后要把代码 push 上去。** 不接 Builds 就意味着
GitHub 与线上是两条各自更新的路径，落差会自己长大；上面第 1 条那个差点回退的场面，
根因就是落差攒到了 6 个 commit 没人发现。

> **git 与 wrangler 对代理的表现是相反的，两者的结论不能混用。** git 配置里有
> `http.proxy = http://127.0.0.1:12334`，`git push` **走这个代理是通的**；wrangler 走同一个
> 代理却必然 `fetch failed`。所以「git 推送成功」推不出「网络直连没问题」，反之也不成立。

## 双域名同步

**涉及域名的改动，两个域名一律同时更新。** 改动点固定 4 处：

| 文件 | 字段 | 填什么 |
| :--- | :--- | :--- |
| `astro.config.mjs` | `site` | **只填主域** `https://duchenlin.top` |
| `src/consts.ts` | `SITE_URL` | **只填主域**（canonical 只能有一个） |
| `wrangler.toml` | `[[routes]]` | **三个域名全列上** |
| `src/content/projects/this-site.md` | 标题与 `link` | 主域 |

改完 `npm run build && npx wrangler deploy`，然后**对两个域名分别验证**。

## 再加一个域名

1. 在 Cloudflare 加站点（Free 套餐），记下它给出的**两台 NS**。
2. 到域名注册商把 NS 改成那两台。
3. 等状态变 Active，然后在 `wrangler.toml` 加一段 `[[routes]]` 并重新部署。

> **NS 是按 zone 随机分配的，两个域名拿到的不是同一对。** 照抄另一个域名的 NS 会导致
> 永远激活不了：那两台服务器上没有你这个 zone 的数据。踩过一次 —— `duchenlin.top` 填了
> `duchenlin.eu.cc` 的 `alfred`/`itzel`，而它自己分到的是 `gloria`/`watson`。
>
> 另外：任何一个 `[[routes]]` 对应的 zone 还是 pending，**整个 `wrangler deploy` 都会失败**，
> 不是只跳过那一条。

## 关于 `duchenlin.eu.cc`

留着当第二入口，但**国内访问不了** —— 有中间设备按主机名字符串匹配注入 TCP RST，
换 IP、换端口、换子域名都绕不过（判据是主机名本身）。境外可正常访问。

这不是配置问题，无法从我们这一侧修复。主域用 `duchenlin.top`。

## 已关闭的 workers.dev 入口

**2026-08-23 关掉了 workers.dev 路由和版本预览 URL 两项**，此后本站只能从三个自定义域名进。

原因是 `<name>.<账号子域>.workers.dev` 里那段**「账号子域」不由我们取名，它的取值本身就是
一条账号标识**（与账号邮箱同类）—— 等于把账号信息挂在一个公开可访问的域名上。这跟
「账号邮箱不写进公开仓库」是同一条原则，只是这次泄漏点在域名而不在文档里。原来留着它的
理由是「DNS 还没生效时用它先验证站点」，三个域名都 Active 之后这个理由已经不存在。

**两个开关是独立的，只关一个不够。** 依据是 wrangler 自己的源码
（`getSubdomainValues()`：`defaultPreviewUrls = undefined`）—— **不写 `preview_urls` 时
`deploy` 压根不去动这个开关**，于是只关 `workers_dev` 会留下
`https://<VERSION_PREFIX>-<name>.<账号子域>.workers.dev` 这条按版本自动生成的公开链接。
wrangler 对这种混合状态会主动发警告，原文：*"you may want to disable the Preview URLs as well
by setting `preview_urls = false`"*。

怎么关的：**先调 API 立刻生效，再改配置文件防止下次部署翻回来。**

```bash
# 立即生效，不需要重新部署（当时工作区有 R14 阶段⑤ 的半成品，部署会把它一起推上线）
curl -X POST -H "Authorization: Bearer $CLOUDFLARE_API_TOKEN" -H "Content-Type: application/json" \
  --data '{"enabled":false,"previews_enabled":false}' \
  "https://api.cloudflare.com/client/v4/accounts/<id>/workers/scripts/duchenlin-blog/subdomain"
```

`wrangler.toml` 里同步写上 `workers_dev = false` 与 `preview_urls = false`。顺带一条：
`defaultWorkersDev = routes.length === 0`，本站有三条 `[[routes]]`，**所以 `workers_dev` 的默认值
本来就是 false**，当初那句 `workers_dev = true` 是主动打开的。

验证：`GET .../workers/scripts/duchenlin-blog/subdomain` 与
`GET .../workers/services/duchenlin-blog/environments/production/subdomain` 两条路径都返回
`{"enabled":false,"previews_enabled":false}`；同一时刻 `duchenlin.top`、`www.duchenlin.top`
与 `/api/music/list` 全部 200，三条自定义域名仍在列。

> **未能核实**：本机连不上 `*.workers.dev`（就是上面「发布流程」里那个 DNS 污染），
> 所以「境外访问那个子域是否真的已经 404」没法自己验 —— 只能凭 API 返回的 `enabled: false`。

## 缓存

`public/_headers` 给哈希文件名的静态资源加了长效缓存头，并给 HTML 加了边缘缓存头。

**已部署实测（2026-08-20 Version `942b25ae` 首测，2026-08-22 Version `d58a3c20` 复验）**：

| 资源 | `Cache-Control` | 实测 |
| :--- | :--- | :--- |
| HTML | `public, max-age=60, stale-while-revalidate=604800` | `CF-Cache-Status: HIT` |
| `/_astro/*`（哈希名） | `public, max-age=31536000, immutable` | 命中（新哈希首次请求是 MISS，第二次起 HIT） |

边缘与浏览器各存 60 秒，过期后 7 天内边缘直接吐旧的并后台刷新，访客不等待。国内首屏
1–5 秒的问题因此解决。

**R14 阶段⑥ 补测掉一条一直挂着的未核实项**（先在本机 `wrangler dev` 8788 上测，
部署后 Version `d58a3c20` 在线上复验，两处结论一致）：`_headers` 里 `/xxx/*` 形式的 splat
**吃空串**，`/posts/` 与 `/home/` 都拿到了上表那套 HTML 头（线上 `CF-Cache-Status: HIT`），
所以栏目首页不必再单独写精确规则。同一轮 curl 顺带照出一个真空档 —— `/music/` 只有
`public, max-age=0, must-revalidate`，也就是没有任何规则命中：`_headers` 从来没给
`/music/*` 写过规则，音乐页吃的是 Workers 静态资源的默认头。站上唯一的常驻功能页
反倒是全站唯一吃不到 60 秒 + 7 天 SWR 的一页。既有遗漏，补法就是照抄一条 `/music/*`，
等沉麟发话。

> **注意 Cache Rules 对本站不生效。** 官方文档：*"No zone configuration for caching applies to
> Workers Caching. Cache Rules... have no effect on a Worker's cache."* 本站是 Workers 静态
> 资源部署，面板上配 Cache Rule 没有用，只能靠响应头。
>
> **`_headers` 管不到 Worker 生成的响应**（官方文档原文 "Custom headers are not applied to
> responses generated by your Worker code"）。原先这条写的是「一旦 R6 阶段②加了 `main`……」
> 的未来式，**现在已经是既成事实** —— 2026-08-22 [R11 点歌台](需求台账.md#r11-点歌台-)
> 加了 `main = "src/worker.ts"`。

### `/api/music/*` 的缓存头（由 Worker 自设）

上面那张表**只管静态资源**。点歌台的五条接口不受 `public/_headers` 约束，缓存头在
`src/worker.ts` 里逐端点写死：

| 端点 | `Cache-Control` | 为什么 |
| :--- | :--- | :--- |
| `/api/music/list` | `public, max-age=300` | **歌单是活的**（R23 从 3600 降下来的：沉麟往「沉麟推荐」里加一首歌，不该等一小时才在站上看见；`wrangler dev` 下这层缓存由 miniflare 持久化到 `.wrangler/state/v3/cache/`，**重启进程也不清**，一小时的 TTL 在本地开发里就是一小时看不见改动） |
| `/api/music/search` | `public, max-age=300` | 同一个词短时间内会被反复搜 |
| `/api/music/lyric` | `public, max-age=86400` | 歌词不变 |
| `/api/music/pic` | `public, max-age=86400` | 封面直链是长期地址（自带 `max-age=31536000`，路径里没有 token 也没有时间戳），缓存它没有「过期了就没声音」的风险 |
| `/api/music/url` | `no-store` | 直链路径里带时间戳。R14 实测同一个 URL 连测 6 分钟仍全是 206，所以「播到一半链接过期」不成立；但**更长的有效期仍未核实**，而且缓存它等于把一条会过期的链接发给后来的人，所以照旧不缓存 |
| 任何失败响应 | `no-store` | 不然会把一次上游抖动缓存一整天 |

前端那边配套改了一处：`api()` 的 `fetch` 带 `cache: 'no-store'`，浏览器一律不吃 HTTP 缓存 ——
不然响应头里那条 `max-age` 对它同样生效，「改了歌单刷新看不到变化」会多出第二层原因。
命中与否从此只由边缘那 5 分钟决定，一处可控。

全部带 `X-Robots-Tag: noindex`。命中走 `caches.default`（Workers 独有的默认缓存区），成功且
非 `no-store` 的响应才 `ctx.waitUntil(edge.put(...))`。改这些值只改代码，跟 `_headers`
和面板都没关系。

**首次带 `main` 的部署已线上实测（2026-08-22，Version `d58a3c20`）** —— 在这次部署之前，
点歌台只在本机 `wrangler dev` 8788 上跑过，「线上是否同样成立」属未核实：

| 请求 | 线上结果 |
| :--- | :--- |
| `/api/music/list?id=3778678` | 200，`public, max-age=3600`，`x-robots-tag: noindex` |
| `/api/music/search?q=…&n=6` | 200，`public, max-age=300`，6 条 |
| `/api/music/lyric?…` | 200，`public, max-age=86400`，35 行带时间戳 |
| `/api/music/url?…`（**不带** `name`） | 200，`no-store`，响应只有 `url` 一个字段（择优上线前的行为原样保留） |
| `/api/music/url?…&name=&artist=` | 200，`bytes: 9550125`（≈9.11 MB），首选本身够大所以走快路径、不启动择优 |
| `/api/music/list?id=999999`（白名单外） | 400，`no-store` |
| `/api/music/nope` | 404 |
| `POST /api/music/list` | 405 |
| 15 条站内静态路径 | 全部 200；不存在的路径 404 |

这张表记的是那一天的线上值。**R23（2026-08-23）之后 `list` 是 `max-age=300`**，`pic` 端点
也还没上线过 —— 上表两处与现在的代码不一致是因为它是历史实测记录，不是当前口径。

`swapped` / `short` 这两个标记**本轮线上没有触发条件**：同一次搜索的 6 条候选逐条探测，
5 条拿到 8.04–9.11 MB 的完整文件、1 条 tencent 取不到直链，没有一条的首选小于 2 MB 阈值。
上游返回片段是**间歇性**的（实测过同一条记录不同时刻返回 720813 B 与 9550125 B 两种结果），
所以这两个分支没法按需复现，只能等它自己出现时再验。

**加了 `main` 之后运维上多的那一件事**：以前构建失败只是不部署、线上不受影响；现在 Worker
抛异常就是线上 500。所以 `src/worker.ts` 里每条路径都有兜底，且非 `/api/music/*` 的请求
一律 `return env.ASSETS.fetch(request)` 原样交回静态资源 —— 静态站的行为一点不变
（已实测：`/` 与不存在的路径分别正常返根路径那一页与 404 页 —— R14 阶段⑥ 之后根路径是起始页，
正站首页在 `/home`）。

### R22–R24 部署实测（2026-08-23，Version `06f81a89`）

```
/                     200；<body data-portal> 在、data-no-chrome 已无、状态栏节点在
/_astro/index.*.css   有 --door-l（门框光）、旧的 circle at 50% 44% 已无
/api/music/list       200，public, max-age=300，323 条，x-robots-tag: noindex
/api/music/pic        200，public, max-age=86400，{"url":"https://p3.music.126.net/…"}
/api/music/pic 漏 song 400 {"error":"缺参数（封面还要 song）"}
/api/music/url        200，no-store（不带 name 的老行为原样）
/audio/te-bie-de-ren.mp3  200，10,365,431 B，audio/mpeg
/audio/te-bie-de-ren.jpg  200，87,364 B，image/jpeg
/audio/door-light.mp3     404（旧音垫已删）
/music/               200；有 jk-vinyl__cover、本地封面已预填、旧两首名字已无
/api/music/nope 404   /no-such-page/ 404
```

这次部署照出两条**以前没记过的线上口径**，都不是本轮改坏的，但都因为本轮才有了实际影响：

**① 静态资源不支持 Range。** 带 `Range: bytes=0-9` 或 `bytes=5000000-5000099` 请求
`/audio/te-bie-de-ren.mp3`，线上一律回 **200 + 整段 10,365,431 字节**，既没有
`accept-ranges` 也没有 `content-range`（`cf-cache-status: HIT`）。图片同理。于是本地这首歌
是**整段下载**的：`preload="none"` 保证点播放之前不下载，点了之后一次拉完 10.4 MB，
拖进度条到还没到的位置要等那一段流过去。以前本地是两段几百 KB 的音垫，这条限制看不出来。
点歌进来的曲目不受影响 —— 那些字节直接从 `m801.music.126.net` 拉，上游支持 206（实测过）。
真要按需分段，只有把音频挪去 R2 并自己写 Range 分支这一条路，而那等于让 Worker 中转媒体
字节，与既定设计相反 —— 先记着，别顺手改。

**② 边缘缓存命中时 `cache-control` 会被改写成 4 小时。** Worker 生成的响应带
`public, max-age=300`，本身是可缓存的，所以它**确实会进边缘缓存**（与「Cache Rules 对
Workers 不生效」是两回事 —— 那说的是规则配不进来，不是默认缓存不工作）。实测：

```
list?…&probe=1..5（每次 URL 不同）  → max-age=300，  cf-cache-status: null
search?q=方大同&n=3 第一次           → max-age=300，  cf-cache-status: null
search?q=方大同&n=3 第二次           → max-age=14400，cf-cache-status: HIT
```

14400 秒＝4 小时，是 zone 上 Browser Cache TTL 的默认值：**HIT 的响应会被它把
`max-age` 抬上去**（`pic` 的 86400 比它长，就原样保留，所以不是无条件覆盖）。
边缘自己的 TTL 仍按源的 300 算，站上表现也不受影响 —— 前端 `api()` 一律
`cache: 'no-store'`，浏览器既不读也不写这条缓存（R23 加的那一半正好挡在这儿）。
但**这条头是给浏览器看的**：哪天前端把 `no-store` 去掉，「改了歌单四小时看不到」
就会以线上独有的形式回来。要根治得去面板把 Browser Cache TTL 改成
「Respect Existing Headers」，那是 zone 设置，等沉麟发话。

### 随笔「与我那个球的故事」上线实测（2026-08-30，Version `47508a13-5940-45e8-b964-3eb3e3d2c1d6`）

纯内容轮次，一篇新随笔，没动路由、`getStaticPaths` 与 `_headers`。提交 `37dd27a`
（2 files，+33 / −1）。包体 273.90 KiB / gzip 76.29 KiB，Worker 启动 5ms，
`Uploaded 20 files (187 already uploaded)`。

`npm run build` **76 页**（判据从 73 改到这个数：新增 notes/story-of-my-orb 一篇 ×
三种语言 = +3），内容索引 45 个文件（notes 从 2 变 3），`astro check` 0 错误 0 警告 2 hint。

标准清单（点歌台五条、素材字节、四条错误分支、门上的旗与状态栏）一字不差，不重复贴。
本轮实测通过的判据：

```
三语全量回归        sitemap 72 条（24 × 3 语）全部 200，<html lang> 全部正确
                    新随笔那三个地址第一次 deploy 就全 200
新随笔三语标题      zh 与我那个球的故事 ／ en The story of that orb and me
                    ／ ja あの球と私の話 —— 三份都是各自语言的标题
正文回落            en 与 ja 页面渲染中文原文，并挂 onlyInChinese 那条提示；
                    中文页没有提示（zh.ts 里那条是空串，符合设计）
四个索引面          notes 列表、归档、标签，三语各三共九个页面都出现新随笔
RSS                 三份各自 <language> 正确（zh-CN / en / ja），且都含新随笔
sitemap             新随笔 12 处（3 个 loc ＋ 每条 3 个 xhtml:link）
语言切换键          三条 data-astro-reload 都在，指向新随笔的三语地址
hreflang            head 里四条（zh-CN、en、ja、x-default）＋ 语言切换三条
```

**没有新坑，两条是对已有记录的加强：**

1. **少量新增第一次 deploy 就全 200，第二次印证。** 本轮新增 3 个页面（1 篇 × 3 语言），
   与 2026-08-28 那轮同量级，同样一次成功。R46 那条「整批 404」的触发条件是**一次新增
   几十个资源**，这个收窄后的结论现在有两轮实测支撑，可以按定论用了。
2. **`index.*.css` 探针本轮有货**（5870 字节，与 2026-08-28 同值 —— 产物名是
   `index.CnZmNP19.css`，两轮之间首页那批 CSS 没变）。仍按「时好时坏、不是硬判据」对待。

顺带记一件仓库卫生的事：工作区里有个未跟踪的 `.agents/skills/deploy/SKILL.md`
（2026-08-26 的旧副本，10724 字节，与 `.claude/skills/deploy/SKILL.md` 的现版已经不同）。
这轮**没有**提交它、也没动它 —— 但它每次 `git status` 都会冒出来，而且是一份会误导人的
过时流程。删掉或者进 `.gitignore`，等沉麟发话。

### 第七篇文章＋全站清标点上线实测（2026-08-28，Version `2a29ed6f-7124-40f4-b913-6ffada2dbf4e`）

内容轮次，没动路由与 `getStaticPaths`。提交 `643e735`（36 files，+432 / −195）。
包体 268.12 KiB / gzip 75.00 KiB，Worker 启动 13ms，`Uploaded 64 files (140 already uploaded)`。

`npm run build` **73 页**（判据从 70 改到这个数：新增 posts/orb-nine-revisions 一篇 ×
三种语言 = +3。以后加一篇文章就是 +3，加一种语言是 +23），`astro check`
0 错误 0 警告 2 hint。

标准清单（点歌台五条、素材字节、四条错误分支、门上的旗与状态栏）一字不差，不重复贴。
本轮实测通过的判据：

```
三语全量回归        23 条路由 × 3 种语言 = 69 条全部 200，<html lang> 全部正确
                    （含新文章的三个地址，第一次 deploy 就全 200）
清标点的线上核对    非日语页面 grep 不到 「，中文正文页 <strong> 为零
                    入口页新文案已上线、旧措辞无残留
新文章三语标题      zh / en / ja 三份都是各自语言的标题
hreflang            head 里四条（zh-CN、en、ja、x-default）＋ 语言切换三条
RSS                 三份各自 <language> 正确，且都含新文章
sitemap             207 条 xhtml:link，含新文章
语言切换键          三条 data-astro-reload 都在
kb.json             8441 B，「 与 ** 各 0 处，换过的措辞已上线
/admin/             200
```

**这一轮照出三条口径，都是对已有记录的修正，不是新坑：**

1. **R46 那条「第一次 deploy 整批 404」没有复现。** 本轮新增 3 个页面（1 篇 × 3 语言），
   第一次 deploy 之后立刻全部 200。所以那条口径的触发条件要收窄成**一次新增几十个资源**
   （R46 是 82 个新资源），不是「只要有新页面就会」。少量新增照常一次成功，不必预先
   准备第二次 deploy。
2. **`index.*.css` 那条探针又有效了**（返回 5870 字节）。R44＋R45 那轮记的是「已失效」——
   实际是**时好时坏**，取决于首页那一批 CSS 的产物名。它不能当硬判据：拿不到就换成
   从页面 HTML 里正则出实际文件名再取，别因为它空了就以为部署坏了。
3. **分身提示词的改动无法在线验证。** 这轮把 `data/voice.ts` 与 `data/chat.ts` 里的
   `「」`／`**` 清了，并显式写进禁令（chat.ts 第 7 条、voice.ts 语气规则最后一条），
   `voiceBlock()` 的例句分隔符从 `「x」、「y」` 换成 `｜`。提示词编译进 Worker、
   按设计不外泄，而**模型是否真的照办**要问分身一句真话 —— 本机打不通上游，线上验它
   要么花额度要么走后台接管（需沉麟登录）。**已验的只有「改动进了这次部署」，
   没验「分身不再输出这两个符号」。** 这一条挂在待验清单上。

### R46 多语言上线实测（2026-08-27，Version `a6995427-1f8d-43c3-aa60-2c144d4f538f`）

**这一轮照出一条会咬人的新口径，先写它**（详见下面「一次部署新增几十个资源」那小节）：
第一次 `deploy`（Version `15b85dea-d3e0-430e-bdd2-bc14c744d461`）输出完全正常
——「Uploaded 82 files (119 already uploaded)」、三个域名触发器齐全 —— 但线上
`/en/*` 与 `/ja/*` **全部 404**，中文那批一条不少地 200。**同一份 `dist/` 再 deploy 一次，
15 秒后全部 200**。所以那一版实际上线的是 `a6995427`。

`npm run build` **70 页**（判据从 24 改到这个数：22 个页面 × 3 种语言 ＋ /admin ＋
/start 跳转页 ＋ 根 404 ＋ 3 份 RSS），`astro check` 0 错误 0 警告 2 hint。
提交 `3f929d9`（69 files，+3804 / −794）。包体 235.69 KiB / gzip 66.40 KiB，
Worker 启动 5ms，`dist/` 共 201 个可上传资源。

标准清单（点歌台五条、素材字节、四条错误分支、门上的旗与状态栏）一字不差，不重复贴。
本轮新加的判据，全部实测通过：

```
中文旧地址回归      18 条全 200（/、/home/、四个栏目与它们的详情页、/rss.xml、
                    /llms.txt、/start/、/kb.json、/404.html）—— 一个地址都没变
三语页面            12 条 200 且 <html lang> 正确（zh-CN / en / ja）
hreflang            /en/posts/ 上四条：zh-CN、en、ja、x-default，canonical 指自己
og:locale           en_US ＋ 两条 og:locale:alternate
RSS                 三份各自 <language>（zh-CN / en / ja），首条标题按语言译过
                    /en/posts/ 的 <link rel=alternate rss> 指 /en/rss.xml
sitemap             66 条 <url>，每条带三条 xhtml:link 互指
_headers 新规则      /en/ /en/posts/ /ja/ /ja/posts/ 全部 public, max-age=60,
                    stale-while-revalidate=604800（与中文那批同档）
语言切换键          三条 <a> 都带 data-astro-reload，当前语言那条 aria-current="true"
元数据译文          /en/posts/ 的 <title> 是 Technical Writing、条目标题是英文
「只有中文原文」    未译那篇有 .original 提示条；已译那篇（/en/projects/data-agent-study/）
                    没有提示条且正文是英文
llms.txt            那段「站上有三种语言」的说明在（9714 B）
/api/chat 的 lang    空消息分支三语各自正确、未知值 xx 落回中文（不花模型额度的验法）
```

**没能在这一轮验到的**：分身**真实回答**的语言。上游那条链路在本机 `wrangler dev` 里
打不通（出网被拦，与 workers.dev 那条同一台机器上的同类问题），而线上验它要么花模型
额度、要么走后台「接管」当探针 —— 后者需要沉麟登录后台。请求形状、`lang` 白名单、
三语错误文案、以及 `buildSystem()` 生成的提示词（用 esbuild 把 `data/chat.ts` 打成
mjs 再 `node` 里调，三种语言的语言指令与 `/about` 地址都对）都验过了，**缺的只有最后
一句话是什么语言**。

#### 一次部署新增几十个资源之后，第一次可能整批 404 —— 再 deploy 一次

排查经过（这些排除项都是实测的，别再重复走一遍）：

| 排除项 | 实测 |
| :--- | :--- |
| 漏传？ | `dist/` 201 个可上传资源，部署报告 82 新 ＋ 119 已有 = 201，**数字对得上** |
| 单域名缓存？ | `duchenlin.top` 与 `www.duchenlin.top` 都 404（两者缓存键不同） |
| 浏览器/边缘缓存？ | 带唯一 query ＋ `Cache-Control: no-cache` 仍 404 |
| `cf-cache-status: HIT` 是这个 URL 命中了？ | **不是** —— 那是回落给它的 `404.html`
  这个资源的缓存状态。别被它带偏，它会让人以为「purge 一下就好」 |
| 传播延迟自愈？ | 等了 5 分钟没变 |
| `_headers` 里新加的 `/en` 规则写坏了？ | 不是。`_headers` 只影响响应头，而且中文那批的
  `cache-control` 一直是对的 |
| Worker 拦了？ | 不是。`run_worker_first` 只列了三条 `/api/*`，中文页面同样不过 Worker 而它们 200 |

结论：**资源清单（manifest）在边缘没切过去**，重跑一次 `npx wrangler deploy`
（同一份 `dist/`，wrangler 会重新提交清单）即刻生效。

**所以验线上发现新页面整批 404 时的正确动作是：先原样再 deploy 一次，不要回滚、
不要改代码、也不要去 purge 缓存。** 只有第二次也 404 才值得往下查。

**⚠ 2026-08-28 补一条边界**：这个现象要**一次新增几十个资源**才出得来。那次新增
3 个页面（一篇文章 × 三种语言），第一次 deploy 之后线上立刻全部 200。所以少量新增
不必预先准备第二次 deploy —— 照常验，真 404 了再重跑。

### R44＋R45 上线实测（2026-08-27，Version `85ae4b6d-fd3c-44f0-9460-2db83343c8fa`）

上传 33 个文件（120 个已在）、三个自定义域名触发器齐全、Worker 启动 6ms、
包体 208.62 KiB / gzip 58.56 KiB（与上一轮同）。`npm run build` **24 页**、
`astro check` 0 错误 0 警告 2 hint。提交 `ca5319b`（35 files，+3202 / −960）。

这一轮换的是视觉骨架（「对开大版」＋四套版次）与左下角的对话入口，标准清单
（点歌台五条、素材字节、四条错误分支）与前三轮一字不差，不重复贴。本轮新加的判据：

```
/home 键面文字      「问沉麟的分身」在 HTML 里（不是 title 属性，是可见文字）
/home 提示纸条      data-ai-hint 在；文案「关于我、这个站、我在做的东西，问它。」
/home 开场三问      data-ai-ask ×4 —— 你平时做什么？／这个站是用什么做的？／你最近在学什么？
/home 版次切换器    role="radiogroup" 在，data-edition 色卡 6 处
/home 刊头期号      第 12 号
/home 竖排栏目标识  .vlabel ×3 —— 文章／随笔／作品
CSS 四套版次        [data-theme= 命中 17 处；--seal / --press / --glass-under / --shadow-glass 全在
CSS 旧账清零        硬编码 rgba(0,0,0,.75) 已无（三处阴影都换成派生的 --shadow-glass）
```

#### 一条这轮照出来的线上口径

**`/home` 的样式已经不在 `index.*.css` 一个文件里了。** 这一版是三份：
`BaseLayout.*.css`（32499 B，骨架与版次 token 都在这里）、`home.*.css`（6047 B）、
`MiniPlayer.*.css`（20813 B）。上面 R22–R24 那条判据用的 `/_astro/index.*.css`
探针在这一版**已经不成立** —— 拿 CSS token 验上线，必须先从 HTML 里正则捞出所有
`/_astro/*.css` 再合并起来搜，只抓其中一份会漏掉一多半，然后得出「token 没上线」
的错误结论。

### 作品集与技术文章上线（2026-08-26，Version `e8ace275-c52d-4f60-b475-46ccedc77dbe`）

上传 18 个文件（134 个已在）、三个自定义域名触发器齐全、Worker 启动 4ms、
包体 208.62 KiB / gzip 58.56 KiB。`npm run build` **24 页** —— 比上一轮多 8 个
（4 个 `/posts/*` 加 4 个 `/projects/*`），deploy 技能里那条判据已同步改成 24。
`astro check` 0 错误 0 警告 2 hint（`content.config.ts` 两处 `z.string().url()` 弃用提示）。

标准清单（点歌台五条、素材字节、四条错误分支）逐条与代码里 `CACHE` 那张表对上，
与前两轮一字不差，不重复贴。本轮新加的判据：

```
8 个新页面      全 200，逐页抽验正文关键内容都在
               （669 passed / 90.20 / is not True / UNKNOWN_SUCCESS / REAL-AGENT）
/projects/     共 5 个，五条标题齐
/posts/        共 6 篇，四篇新标题齐
/rss.xml       四篇新文章 4/4 进 feed
/archive/      新条目进时间线，全站日期分散在 05-04 到 08-26
/tags/         Python / 语音 / Agent / SQLite / FFmpeg 五个新标签都在
/kb.json       26 条；my-projects 与 what-im-learning 的新口径都已生效
```

#### 两条这轮照出来的口径

**`/kb.json` 的字段是 `q` / `aliases` / `topic` / `a`**，顶层是 `{count, items}` ——
不是 `question` / `answer`。拿它当验收探针时按这个名字匹配，否则会数出 0 条却以为内容没进去。

**改知识库必须走完整上线流程。** `/kb.json` 是构建期从 `src/content/kb/` 生成的静态页，
`src/data/content.generated.ts` 也是 `npm run build` 的产物 —— 两者都只在 `wrangler deploy`
之后才换。只提交不部署的话，站上的 AI 分身答的仍是旧口径，而仓库看起来已经改好了。

### R41＋R42 部署实测（2026-08-25，Version `62a63ff7-9ae1-4db3-8e38-4bd8bb848170`）

上传 18 个文件（126 个已在）、三个自定义域名触发器齐全、Worker 启动 8ms、
包体 140.77 KiB / gzip 39.45 KiB。`npm run build` 16 页、`astro check` 0 错误 0 警告。

标准清单（点歌台五条、素材字节、四条错误分支）逐条与代码里 `CACHE` 那张表对上，
与上一轮一字不差，不重复贴。本轮新加的判据：

```
/notes/ 的两份 CSS   body{flex-direction:column} 在、100dvh 在、main{flex:1 0 auto} 在
/admin/              200，noindex, nofollow，no-store；五个面板名齐；
                     HTML 里扫不到密钥形状，也扫不到任何上游域名（预设表在 JS 里）
/api/admin/state     未登录 → 200 {"logged":false,"ready":true}
/api/admin/stat      未登录 → 401 {"ok":false,"error":"进不去"}
/about/              tencent://AddContact 在、mqqapi://card/show_pslcard 在、
                     wpa.qq.com 已经没有了
/api/chat            不传窗口号 / 窗口号太短 → 400「窗口号不对」（证明新 Worker 在跑，
                     且这两条不花模型额度、不落库）
```

#### 十家国内服务商，从 Cloudflare 边缘**全部可达**

这是本轮最重要的线上结论，它直接回答了「我必须能使用国内的服务商」：**能，直连就行。**

后台那颗「探一下能不能连」逐家打过（`colo=LAX`，出网在洛杉矶）：

| 服务商 | 结果 | 服务商 | 结果 |
| :--- | :--- | :--- | :--- |
| DeepSeek | 401 · 0.3s | 腾讯混元 | 401 · 2.5s |
| 阿里百炼 | 401 · 1.2s | 百度千帆 | 403 · 1.6s |
| 智谱 GLM | 401 · 0.7s | MiniMax | 401 · 1.1s |
| 月之暗面 Kimi | 401 · 2.1s | 讯飞星火 | 401 · 1.3s |
| 硅基流动 | 401 · 0.9s | 火山方舟 | 401 · 0.8s |

十家全部拿到 HTTP 状态码（401 = 主机在、路径对、只是没给密钥），最慢 2.5 秒。

**所以 R37 那条口径要按这个改**：不是「Cloudflare 边缘打大陆机器 30 秒无响应」，
而是**那一家中转站在区别对待 workerd 的请求**。前者是被误推广的结论，
它在代码注释里躺了好几轮，害得「换国内服务商」这条路一直没人试。
要用国内服务商，去哪家注册一把密钥填进后台就行，不必换域名、不必挂中转。

#### 两条新的验收工具口径

1. **`/cdn-cgi/trace` 在自定义域名上可用** —— 返回 `ip=`（Cloudflare 侧看到的出口 IP）、
   `colo=`（接入机房）、`loc=`。这是拿这两个值最省事的办法，不用另找 IP 回显服务
   （那些会因为本机代理而给出不同的答案）。

2. **用「接管」当 R42 的探针，不花模型额度。** 会话号是服务端按 IP 算的，客户端看不到它 ——
   那怎么验「算对了」？办法是：本地按同一个公式算一遍（`/cdn-cgi/trace` 拿 IP →
   `SHA-256('ip:'+IP)` 前 16 字节），拿这个会话号去后台开接管，然后发一句话。
   **那句话如果真被挂住了，就说明服务端算出的会话号与本地一模一样** ——
   接管这条路整个不调模型，所以这个验证零上游成本。

   实测：本地算出 `fb39f628…` → 开接管 → 窗口 A 发一句 → 2.5 秒后后台看到「有人在等」、
   `n = 1` → 回一句 → 窗口 A 在 3.7 秒收到原话。换窗口 B 再走一遍，两条落在**同一条会话**里、
   窗口号各自分开（记录里四条消息，`user/ai` 与 `ai/human` 交替，窗口列 A A B B）。

#### 顺手查清一件事：那条 530 不是协议配错

进后台看到 `lastWhy` 是「主路 claude-opus-5：上游 HTTP 530」，而协议配的是 `anthropic` ——
长得**很像** R40 那个坑（协议选成 anthropic、打到 `/v1/messages` 得到 5xx）。
拿测试台两种协议各打一次（只测不保存）：

| 协议 | 结果 |
| :--- | :--- |
| `anthropic`（他现在配的） | **可用** · HTTP 200 · 7.3s · 提示词 16268 token · 回答完全是他的语气 |
| `openai` | 连不上 · 「上游返回的不是 JSON」 |

**反过来了：他配的是对的。** 那条 530 是那一次上游抖动（记录时间早于这次实测），
不是配置错。**协议长得像的坑不等于就是那个坑** —— 测一次比猜一次快。

顺带确认主路现在是好的：知识库 16268 token 确实送到了，模型也认角色。

### R26（第二～六轮）＋R27–R29 部署实测（2026-08-23，Version `0b91961a`）

上传 25 个文件（166 个已在）、三个自定义域名触发器齐全、Worker 启动 4ms。
`npm run build` 18 页、`astro check` 0 错误 0 警告。

```
/                     200；<body data-portal> 在、状态栏节点在
/_astro/index.*.css   200，5,848 字节
/api/music/list       200，public, max-age=300
/api/music/pic        200，public, max-age=86400
/api/music/url        200，no-store
/audio/te-bie-de-ren.mp3  200，10,365,431 B
/audio/te-bie-de-ren.jpg  200，87,364 B
/music/  200 有 jk-vinyl__cover      /home/  200 有 site-header
/api/music/nope 404   /no-such-page/ 404   /audio/door-light.mp3 404
/api/music/pic 缺 song 400
/about/               200，16,198 字节；card__qrbox ×4、card__qrbox--paper ×1（抖音）、
                      --qr: url() ×3、lb__stage ×3；lb__flip／「加白底」／
                      lb__stage--classic／card__qrtile／<img class=card__qr> 全部已无
/contact/wechat-qr.webp    200，1,824 B，image/webp
/contact/telegram-qr.webp  200，1,724 B，image/webp
/contact/douyin-qr.webp    200，11,508 B，image/webp
```

**照出一条以前没记过的线上口径**：`/contact/*.webp` 的响应头是
`public, max-age=0, must-revalidate` —— 那是**没有哈希的 `public/` 资源的默认值**，
`public/_headers` 里只给了 `/_astro/*` 长缓存、给 HTML 短鲜期，二维码这类
「内容稳定、每次访问 /about 都要」的图落进了默认档，于是每次都要回边缘验一次
（拿回 304，但仍是一次往返）。

**没顺手改**，因为它是个有代价的选择：给 `/contact/*` 加长缓存 + `immutable` 之后，
哪天换掉某张码（同名覆盖）老访客最长一年拿不到新的 —— 要么接受这个代价，要么给
文件名带上版本号。等沉麟发话。三张图合计 14.7KB，眼下这条只是多几次 304。

## 五个 secret：对话上游与后台口令

R6 阶段②（分身）与 R20②（后台）都靠 secret，**一个都不进仓库**。首次部署前一次配完：

```bash
npx wrangler secret put CHAT_BASE
```

```bash
npx wrangler secret put CHAT_KEY
```

```bash
npx wrangler secret put CHAT_MODEL
```

`CHAT_MODEL` 可以写成**逗号分隔的多个候选**（`模型甲, 模型乙`），按顺序试，第一个挂了
自动换下一个 —— 这不是过度设计，是台账 R34 记的实测：那家上游的模型可用性会漂移，
同一个模型 20 分钟前还好，再调就「无可用渠道」。三样的值都在本机 `.dev.vars` 里
（那个文件已被 `.gitignore` 排除，`git check-ignore` 验过）。

**R41 起这三个 secret 只是底座。** 后台可以配**最多三条线路**（各有自己的端点、密钥、
协议、模型），按顺序试；它们存在 Durable Object 里，改完下一次对话立刻生效。
这三个 secret 只在「后台那份配置为空或存坏了」时兜底 —— 换服务商不必再动 secret，
更不必重新部署。国内服务商的端点表在 `src/data/providers.ts`（十家，端点实测过），
后台「探一下能不能连」那颗按钮回答「Cloudflare 边缘到这一家通不通」。

后台那把锁两样。**明文口令只出现在你自己的终端里**，哈希才进 secret：

```bash
node -e "const c=require('crypto');const s=c.randomBytes(16);const h=c.pbkdf2Sync(process.argv[1],s,10000,32,'sha256');console.log('pbkdf2$10000$'+s.toString('base64')+'$'+h.toString('base64'))" 你的口令
```

```bash
npx wrangler secret put ADMIN_HASH
```

```bash
npx wrangler secret put ADMIN_SECRET
```

第一条打印出 `pbkdf2$10000$…$…`，粘给 `ADMIN_HASH`；`ADMIN_SECRET` 随便粘一串足够长的
随机字符（cookie 签名用，换掉它等于把所有登录态一次性失效）。**两样缺一样，`/admin`
就锁死而不是开着** —— 页面会显示「还没设口令」并把这三条命令列出来。

迭代数只有一万，是因为 Workers Free 是 10 ms CPU／请求，而这把锁的威胁模型里哈希本身
不可能泄漏（只在 secret 里，不落库、不进仓库），离线爆破的前提不成立。

改口令：重跑生成命令 + 重新 `secret put ADMIN_HASH`，**不需要重新部署**。

## 站长认证文件（微信等平台）

平台要求「在网站根目录放一个指定文件」来证明站长身份时：

1. 文件丢进 `public/`，构建时会原样拷到 `dist/` 根 —— **`public/` 就是本站的「网站根目录」**。
2. 检查 `public/_headers` 里有没有规则会命中它。当前 `_headers` 没有 `/*` 兜底规则
   （只有精确的 `/` 和各 HTML 路径前缀），所以根目录的 `.txt` 不会被加上干扰响应头。
3. **必须 `npm run build && npx wrangler deploy`** 才生效 —— 放进 `public/` 不等于上线。
4. 用 `curl` 验证线上可访问且内容逐字正确，再去平台点提交。

已放置的：`public/97832a429e3a27b33742f8b6a9b69e2c.txt`（微信站长认证，2026-08-20 部署，
线上 200 且内容正确）。这类文件平台验证通过后可以删，但删了将来复验要重放，留着成本几乎为零。

## 仓库公开前的脱敏

公开的是 **`DUEDCL/blog`**（2026-08-20 新建）。原来的 `DUEDCL/duchenlin-blog` **保持私有**
当归档 —— 为什么要换一个仓库而不是把旧的改公开，是这一节最值得看的部分，见「force push
不等于删除」。

**公开是不可逆的** —— 内容一旦进公开索引，就算之后改回私有也可能已被缓存或抓取。所以改
可见性之前必须先扫一遍，而且**必须连 git 历史一起扫**：删掉工作区里的那一行不够，历史里
的照样翻得出来。

**当时扫出来的东西**：

| 位置 | 内容 | 处理 |
| :--- | :--- | :--- |
| `docs/部署运维.md`（未提交） | Cloudflare 账号邮箱 —— 等于把 QQ 号挂到公开索引上 | 删掉，账号是哪个不写进仓库 |
| `docs/部署运维.md`（未提交） | 本机绝对路径 `C:\Users\<用户名>\...` | 改成 `%APPDATA%\...`，信息量不变、不暴露用户名 |
| `docs/需求台账.md`（**已在历史里**） | 需求原文里的地名 | 替换为 `〇〇`，历史一并重写 |
| `docs/需求台账.md`（**已在历史里**） | 需求原文里的本机头像路径 | 替换为 `<本机头像路径>`，历史一并重写 |
| `src/content/kb/where-i-live.md`（未提交） | 提示文字里引用了那个地名 | 改成不点名 |

### 怎么扫

```bash
# 工作区：本机路径、地名、手机号、邮箱、密钥类字样
grep -rnE '[A-Za-z]:\\(Users|Downloads)|\b1[3-9][0-9]{9}\b|[\w.]+@(qq|163|gmail)\.com' . | grep -v node_modules
grep -rniE 'api[_-]?token|api[_-]?key|secret|password|bearer [A-Za-z0-9_-]{20,}' . | grep -v node_modules

# git 全历史：逐个提交搜，这才是准的
git rev-list --all | xargs git grep -l '要搜的字符串'

# 曾被提交过的凭据类文件（即使现在已删）
git log --all --pretty=format: --name-only --diff-filter=A | sort -u | grep -iE '\.env|credential|secret|\.pem$|\.key$'
```

> **别用 `git log -S` 当扫描手段。** 它只列出**改变了该字符串出现次数**的提交。本次实测：
> `git log --all -S'<地名>'` 只报出 1 个提交，而 `git rev-list --all | xargs git grep -l` 报出
> **8 个** —— 字符串首次引入后一直存在，后续每个版本里都还有，`-S` 一个都不报。
> 据 `-S` 的结果判断「只有那一个提交受影响」会直接漏掉七个。

> **`git grep` 搜历史时不能加 `--`。** `git rev-list --all | xargs git grep -l 'x' --` 会把
> 后面的 SHA 全部当成**路径**（`--` 是「版本到此为止」的分隔符），结果恒为 0 命中，看起来
> 像「历史很干净」。正确写法是不带 `--`。这个假阴性踩过一次。

### 历史重写

本机没有 `git filter-repo`（也没有对应 python 模块），用内置的 `filter-branch`。替换脚本
放在**仓库外**，否则会被 `--tree-filter` 一起提交进去：

```bash
# /tmp/scrub.sh —— 只处理 .md，sed -i 碰二进制会损坏文件
find . -name '*.md' -type f -exec sed -i -e 's/旧串/新串/g' {} +

git bundle create /tmp/backup.bundle --all   # 先备份，并 git bundle verify 一次
FILTER_BRANCH_SQUELCH_WARNING=1 git filter-branch --force --tree-filter 'sh /tmp/scrub.sh' -- --all
```

两个必须知道的行为：

- **`filter-branch` 会把 `refs/remotes/origin/main` 也一起改写**（`-- --all` 的效果）。本地
  记录的远端状态因此失真，push 前先 `git fetch origin` 拉回真实值。
- **它把原始引用留在 `refs/original/` 下**，所以重写之后 `git rev-list --all` 会同时数到
  新旧两套提交（本次 16 → 25），`git grep` 也会在旧的那套里搜出敏感串。**这不是脱敏失败**。
  验证时要么只看 `git rev-list main`，要么先删备份引用：

```bash
git for-each-ref --format='%(refname)' refs/original | xargs -n1 git update-ref -d
git reflog expire --expire=now --all && git gc --prune=now
```

### force push 不等于删除（这次最重要的发现）

**在 GitHub 上，`git push --force` 不会让旧提交消失。** 它们变成 unreachable object 继续
留在服务端，而且**公开状态下匿名可读**。本次实测，仓库公开后：

| 步骤 | 匿名请求 | 结果 |
| :--- | :--- | :--- |
| 1 | `GET /repos/{owner}/{repo}/events` | 200，响应里直接有 `"head": "<force push 前的 SHA>"` |
| 2 | `GET /repos/{owner}/{repo}/commits/<那个 SHA>` | 200，`parents` 字段完整 |
| 3 | 顺 parent 链往上走 | 走到任意一个被重写掉的旧提交 |
| 4 | `raw.githubusercontent.com/<owner>/<repo>/<旧 SHA>/<文件>` | 200，**未脱敏原文原样返回** |

也就是说**不需要猜 40 位 SHA**，调一次 events API 就拿到入口。重写历史的工作在 GitHub 侧
等于白做。

改回私有可以立即止血（上面 1–4 全部变 404），但有个尾巴：`raw.githubusercontent.com` 走
Fastly，响应带 `Cache-Control: max-age=300`，**私有化之后 5 分钟内仍能从 CDN 缓存读到**
（实测 `X-Cache: HIT`；等 `Expires` 过后复验为 `X-Cache: MISS` + 404）。

**结论：想让敏感内容真正不可达，只能换一个从来没存过它的仓库。** 两条路 ——

1. 删掉远端仓库再同名重建（彻底，但删除不可逆）；
2. **新建另一个仓库推干净的历史，旧仓库保持私有**（本次选的路，零删除风险，代价是仓库地址变了）。

选 2 之后要跟着改仓库地址的地方：`src/content/projects/this-site.md` 的 `repo`、
`src/content/kb/is-code-open.md` 的正文、本文档「发布流程」里 Workers Builds 要连接的仓库。
`src/consts.ts` 的 `SOCIAL.github` 指向的是用户主页不是仓库，不用改。

**验收判据不是「本地干净」，是匿名 clone 干净**：

```bash
GIT_TERMINAL_PROMPT=0 git -c credential.helper= clone https://github.com/<owner>/<repo>.git /tmp/verify
cd /tmp/verify && git rev-list --all | xargs git grep -l '敏感串'   # 必须 0 命中
```

**以后的约定**：`docs/` 是公开文档。账号邮箱、本机绝对路径、地名、二维码图片一律不写进去 ——
这类信息对读文档的人零价值，对搜到它的人有价值。「需求原文一律保留」这条原则的目的是不丢
约束，而地名和本机路径不构成任何约束，所以脱敏不违反它。

## 踩过的坑汇总

| 现象 | 根因 |
| :--- | :--- |
| `wrangler deploy` 整体失败 | 某个 `[[routes]]` 的 zone 还是 pending |
| 域名永远激活不了 | 照抄了另一个域名的 NS |
| 404 页面不生效 | Workers 不自动认 `404.html`，必须在 `[assets]` 显式声明 `not_found_handling`。Pages 是自动的，这是两者差异 |
| wrangler `fetch failed` | 走了本机代理，需清掉代理环境变量 |
| dashboard 构建失败 | Worker 名与 `wrangler.toml` 的 `name` 不一致 |
| ~~首屏慢~~ | ~~HTML 未做边缘缓存~~ —— 已解决，见「缓存」一节 |
| 推了 GitHub 但网站没更新 | Workers Builds 没接，GitHub 只是代码存放处。上线必须本地 `wrangler deploy` |
| 找不到 wrangler 凭据 | 它在 `%APPDATA%\xdg.config\.wrangler\` 而不是 `~/.wrangler\` |
| PowerShell 报 `unset` 不是 cmdlet／`&&` 不是有效分隔符 | 那是 POSIX shell 语法。PowerShell 用 `;` 分隔、`Remove-Item Env:X` 清变量 |
| 站长认证文件 404 | 放进 `public/` 后没部署。`public/` 的改动同样要走 build + deploy |
| 以为删掉工作区那行就算脱敏了 | git 历史里还在。公开前必须扫历史，见「仓库公开前的脱敏」 |
| `git log -S` 只报了一个提交，误以为只有一处 | 它只列改变了字符串出现次数的提交。实测漏报 7/8，别拿它当扫描手段 |
| 搜历史返回 0 命中，以为很干净 | `git rev-list --all \| xargs git grep -l 'x' --` 里的 `--` 把 SHA 当成了路径。去掉 `--` |
| `filter-branch` 之后还能搜到敏感串 | 旧引用留在 `refs/original/` 下，被 `rev-list --all` 数进去了。不是脱敏失败 |
| force push 之后旧提交还能匿名访问 | GitHub 不删 unreachable object。公开状态下 events API 直接给出旧 SHA，parent 链可遍历。只能换仓库 |
| 改回私有了但 raw 还能读到 | `raw.githubusercontent.com` 走 Fastly，`max-age=300`，5 分钟缓存窗口 |
| GitHub API 建仓库返 `Problems parsing JSON` | 描述里有中文，经 shell 的 `-d` 传递被编码搞坏。改用 `--data-binary @文件`（文件存 UTF-8） |
| 拿 git 推送成功推断出「网络直连正常」 | git 走 `http.proxy` 是通的，wrangler 走同一个代理必挂。两者结论不能互推 |
| 关了 `workers_dev` 以为 workers.dev 入口就没了 | 版本预览 URL 是**另一个**开关，不写 `preview_urls = false` 时 deploy 根本不动它 |
| 部署输出正常，但新加的那批页面线上整批 404 | 资源清单没在边缘切过去。**同一份 `dist/` 再 deploy 一次**即好（R46 实测）。别回滚、别 purge —— 排除过程见 R46 那节 |
| 拿 `cf-cache-status: HIT` 推断「是缓存问题」 | 404 时那个 HIT 是回落用的 `404.html` 的缓存状态，不是被请求的那个 URL 的。带唯一 query ＋ no-cache 仍 404 就与缓存无关 |
| `*.workers.dev` 本机怎么都连不上 | DNS 解析到 `162.125.32.13`（非 Cloudflare 段），直连 TCP 超时、走代理 TLS 握手失败。不是配置问题 |
| 看到 config 里躺着 `oauth_token` 就以为已授权 | 它已失效（`9109 Invalid access token`）。真正在起作用的是环境变量 `CLOUDFLARE_API_TOKEN` |
| 想着「先连上 Builds 再说」 | 远端可能落后本地好几个 commit，连上后一次推送就把线上换成旧代码。必须先 push 再连 |
| `public/` 下的图每次访问都要回源验一次 | 没哈希的 `public/` 资源默认 `max-age=0, must-revalidate`；`_headers` 只给了 `/_astro/*` 长缓存。二维码这类稳定资源要长缓存得自己加规则，代价是同名换图后一年不更新 |
| 以为静态资源能按 Range 分段取 | Workers Assets 不实现 Range：带 Range 也回 200 + 整段。10 MB 的 mp3 是一次下完的，拖进度条到未缓冲处要等 |
| 看到线上 `max-age=14400` 以为代码里写错了 | 那是 zone 的 Browser Cache TTL（默认 4 小时）在 `cf-cache-status: HIT` 时抬高的，源头仍是 300。带个不重复的 query 再打一次就看得到真值 |
| 拿 `/_astro/index.*.css` 当上线探针，token 一个都搜不到 | R44 之后 `/home` 的样式拆成 `BaseLayout` / `home` / `MiniPlayer` 三份，版次 token 在第一份里，`index.*.css` 里没有。要从 HTML 捞出全部 `.css` 再合并搜 |
| `astro dev --port 4341` 打印「already running at 4331」然后 exit 0 | Astro 7 的 dev 守护进程按**项目**单例，不是按端口。换个端口另起一个是不可能的 —— 要么直连已有那个（读页面不冲突），要么先 `astro dev stop`。被这条坑到时的表象是「预览面板报启动成功但 curl 得 000」，以及 Playwright 脚本 `Execution context was destroyed`（其实是导航到了错误页） |
